{
  "name": "brutus",
  "version": "0.3.0",
  "private": true,
  "description": "Auto-battler con mecánicas porteadas de El Bruto / MyBrute, temática de fantasía oscura.",
  "workspaces": [
    "core",
    "server",
    "client",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w core && npm run build -w server && npm run build -w client",
    "start": "npm run start -w server",
    "start:railway": "npm run start",
    "test": "npm run test -w core",
    "prisma:generate": "npm run prisma:generate -w server",
    "prisma:migrate": "npm run prisma:migrate -w server",
    "audit:deps": "npm audit --workspaces --include=dev",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}
